export interface UserTableHeader {
  columnName: string;
  displayName: string;
  class: string;
  dropDown: boolean;
  icon: boolean;
  visible: boolean;
  span: boolean;
  link: boolean;
  button: boolean;
  text: boolean;
  checkBox?: boolean;
}
